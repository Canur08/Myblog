#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../oh-vue-icons/node_modules/vue-demi/bin/vue-demi-fix.js" "$@"
  ret=$?
else 
  node  "$basedir/../oh-vue-icons/node_modules/vue-demi/bin/vue-demi-fix.js" "$@"
  ret=$?
fi
exit $ret
